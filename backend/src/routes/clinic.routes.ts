import { Router } from 'express';

const router = Router();

// Define clinic routes here
// Example:
// router.get('/', (req, res) => { res.send('Get all clinics'); });
// router.get('/:id', (req, res) => { res.send(`Get clinic ${req.params.id}`); });

export default router; 