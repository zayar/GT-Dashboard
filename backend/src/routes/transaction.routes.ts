import { Router } from 'express';

const router = Router();

// Define transaction routes here
// Example:
// router.get('/', (req, res) => { res.send('Get all transactions'); });
// router.post('/', (req, res) => { res.send('Create transaction'); });

export default router; 